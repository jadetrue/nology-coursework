const coaches = ["Andy", "Ollie", "Bex"];

const pets = ["Frankie", "Benson"];

//console.log(pets[1]);

for (index = 0; index < coaches.length; index++) {
    console.log(coaches[index]);
}

for (index = 0; index < pets.length; index++) {
    console.log(pets[index])
}