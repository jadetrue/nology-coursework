// const names = ["Andy", "Bex", "Ollie"];

// const coaches = names.filter((name) => {
//     if (name.includes("e")) {
//         return true;
//     } else {
//         return false;
//     }
// })

// console.log(coaches);

const scores = [9, 10, 18];

const highScore = scores.filter((score) => score > 10);

console.log(highScore)